<Window x:Name="CommonWindow1" x:Class="HardTrainingViews.Views.CommonModule.CommonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="HardTraining" Height="600" Width="800"
        DataContext="{Binding Source={StaticResource Locator}, Path=CommonModule}" WindowStyle="ThreeDBorderWindow" ResizeMode="NoResize">
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Moduły"/>
        </ContextMenu>
    </Window.ContextMenu>
  
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="Moduły">
                    <MenuItem Header="Dane użytkownika" Command="{Binding ShowUserDataCommand}"></MenuItem>
                </MenuItem>
            </Menu>
        </DockPanel>
    </Grid>
</Window>
